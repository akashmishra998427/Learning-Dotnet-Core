@{
    ViewData["Title"] = "Home Page";
    // var emp = ViewData["Employee"] as Employee;
    // var employee = ViewBag.employee;
    // var name = TempData["Employe"] as Employee;

    var employeesFromViewData = ViewData["Employees"] as List<Employee>;
    var employeesFromViewBag = ViewBag.employees as List<Employee>;
    var employeesFromTempData = TempData["Employees"] as List<Employee>;
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<h1 class="text-primary">Data from View Data</h1>

@* @{
    if(ViewData["Employee"]!=null)
    {
        <h2>Employee ID: @emp.EmpID</h2>
        <br />
        <h2>Employee Designation: @emp.Designation</h2>
        <br />
        <h2>Employee EmpName: @emp.EmpName</h2>
        <br />
        <h2>Employee Salary: @emp.Salary</h2>
        <br />
    }
    else
    {
        <h1>No Data Found</h1>
    }
}


<h1 class="text-primary">Data from view Bag</h1>

@{
    if (ViewData["Employee"] != null)
    {
        <h2> Employee ID :@employee.EmpID</h2>
        <br />
        <h2> Employee Designation :@employee.Designation</h2>
        <br />
        <h2> Employee EmpName :@employee.EmpName</h2>
        <br />
        <h2> Employee Salary :@employee.Salary</h2>
    }
    else
    {
        <h1>No Data Found</h1>
    }
}

<h1 class="text-primary">Data from Temp Data</h1>

@{
    if (ViewData["Employee"] != null)
    {
        <h2> Employee ID :@name.EmpID</h2>
        <br />
        <h2> Employee Designation :@name.Designation</h2>
        <br />
        <h2> Employee EmpName :@name.EmpName</h2>
        <br />
        <h2> Employee Salary :@name.Salary</h2>
    }
    else
    {
        <h1>No Data Found</h1>
    }
} *@


<!-- Display Employees using ViewData in a Bootstrap-styled table -->
<h1 class="text-primary mb-3">Data from View Data</h1>
@if (employeesFromViewData != null && employeesFromViewData.Any())
{
    <table class="table table-striped table-bordered table-hover">
        <thead class="thead-dark">
            <tr>
                <th>Employee ID</th>
                <th>Employee Name</th>
                <th>Designation</th>
                <th>Salary</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in employeesFromViewData)
            {
                <tr>
                    <td>@employee.EmpID</td>
                    <td>@employee.EmpName</td>
                    <td>@employee.Designation</td>
                    <td>@employee.Salary</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <h1>No Data Found</h1>
}

<!-- Display Employees using ViewBag in a Bootstrap-styled table -->
<h1 class="text-primary mb-3 mt-5">Data from View Bag</h1>
@if (employeesFromViewBag != null && employeesFromViewBag.Any())
{
    <table class="table table-striped table-bordered table-hover">
        <thead class="thead-dark">
            <tr>
                <th>Employee ID</th>
                <th>Employee Name</th>
                <th>Designation</th>
                <th>Salary</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in employeesFromViewBag)
            {
                <tr>
                    <td>@employee.EmpID</td>
                    <td>@employee.EmpName</td>
                    <td>@employee.Designation</td>
                    <td>@employee.Salary</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <h1>No Data Found</h1>
}

<!-- Display Employees using TempData in a Bootstrap-styled table -->
<h1 class="text-primary mb-3 mt-5">Data from Temp Data</h1>
@if (employeesFromTempData != null && employeesFromTempData.Any())
{
    <table class="table table-striped table-bordered table-hover">
        <thead class="thead-dark">
            <tr>
                <th>Employee ID</th>
                <th>Employee Name</th>
                <th>Designation</th>
                <th>Salary</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in employeesFromTempData)
            {
                <tr>
                    <td>@employee.EmpID</td>
                    <td>@employee.EmpName</td>
                    <td>@employee.Designation</td>
                    <td>@employee.Salary</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <h1>No Data Found</h1>
}
